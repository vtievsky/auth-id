{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Codegen Client",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/auth-id/main.go",
            "env": {
            },
            "args": [
            ]
        }
    ]
}